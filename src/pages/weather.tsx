import React, { useState } from \'react\';\nimport WeatherSelect from \'../components/WeatherSelect\';\n\nconst WeatherPage: React.FC = () => {\n  const [selectedData, setSelectedData] = useState<string>(\'temperature2m\');\n\n  // Mock data for demonstration purposes\n  const mockHourlyData = {\n    time: [new Date(), new Date(Date.now() + 3600 * 1000)],\n    temperature2m: new Float32Array([15, 16]),\n    uvIndex: new Float32Array([3, 4]),\n  };\n\n  return (\n    <div>\n      <h1>Weather Data</h1>\n      <WeatherSelect onSelectionChange={setSelectedData} />\n      <div>\n        <h2>{selectedData === \'temperature2m\' ? \'Temperature\' : \'UV Index\'}</h2>\n        <ul>\n          {mockHourlyData.time.map((time, index) => (\n            <li key={time.toISOString()}>\n              {time.toLocaleTimeString()}: {\' \}\n              {selectedData === \'temperature2m\'\n                ? `${mockHourlyData.temperature2m[index].toFixed(1)}°C`\n                : mockHourlyData.uvIndex[index].toFixed(1)}\n            </li>\n          ))}\n        </ul>\n      </div>\n    </div>\n  );\n};\n\nexport default WeatherPage;
